<?xml version="1.0" encoding="UTF-8"?>
<!--
==============================================================================
ARQUIVO: procNFe_v4.00.xsd
______________________________________________________________________________
Propósito:
  Definir o tipo complexo TProcNFe, que representa o processo completo
  da Nota Fiscal Eletrônica (NF-e) — combinando o XML da NFe e o protocolo
  de autorização retornado pela SEFAZ.

Correção:
  - Evita duplicidade de includes entre leiaute e tipos.
  - Inclui apenas o schema de tipos diretamente (tiposNFe_v4.00.xsd).
  - Mantém compatibilidade com o consolidado (NT 2025.002 / PL_010b v1.30).

Base técnica:
  - Nota Técnica 2025.002 — NF-e v4.00
  - Leiaute PL_010b v1.30
  - Aderência W3C XMLSchema 1.0 (Xerces-J)
  - Compatível com execução via Maven, Docker e ambiente empacotado (JAR)

Autor:
  Bruno Ribeiro — Desenvolvedor Fullstack / DevSecOps
  Revisão 3.2.0 — Remoção de loop de includes / Validação estável Xerces
==============================================================================
-->

<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"
           xmlns="http://www.portalfiscal.inf.br/nfe"
           targetNamespace="http://www.portalfiscal.inf.br/nfe"
           elementFormDefault="qualified"
           attributeFormDefault="unqualified">

    <!-- =============================================================
         1. INCLUSÃO — Tipos principais da NF-e
         =============================================================
         Inclui apenas o schema de tipos nacionalizado, evitando
         dependências circulares com leiaute ou consolidado.
    -->
    <xs:include schemaLocation="tiposNFe_v4.00.xsd"/>

    <!-- =============================================================
         2. DEFINIÇÃO — Tipo complexo TProcNFe
         =============================================================
         Representa o processo completo da NF-e, composto pelo XML
         original da nota e pelo protocolo retornado pela SEFAZ.
    -->
    <xs:complexType name="TProcNFe">
        <xs:annotation>
            <xs:documentation>
                Tipo que define o processo completo da NF-e,
                formado pela NFe e pelo protocolo de autorização.
            </xs:documentation>
        </xs:annotation>
        <xs:sequence>
            <xs:element name="NFe" type="TNFe"/>
            <xs:element name="protNFe" type="TProtNFe" minOccurs="0"/>
        </xs:sequence>
        <xs:attribute name="versao" type="xs:string" use="required"/>
    </xs:complexType>

    <!-- =============================================================
         3. ELEMENTO RAIZ — procNFe
         =============================================================
         Define o elemento raiz que representa o processo completo
         da NF-e, combinando o XML da nota e o protocolo da SEFAZ.
    -->
    <xs:element name="procNFe" type="TProcNFe">
        <xs:annotation>
            <xs:documentation>
                Elemento raiz que representa o processo completo da NF-e,
                contendo o XML da nota e o protocolo de autorização emitido
                pela SEFAZ.
            </xs:documentation>
        </xs:annotation>
    </xs:element>

</xs:schema>
